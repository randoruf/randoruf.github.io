---
layout: post
title: "VLC playback speed"
date: 2020-11-21T00:20:00Z
---



VLC 有**热键系统 (hotkey or shortcut)** 。

可以在 Preferences -> Hotkey 处， 找到 Faster (fine) 和 Slower (fine)， 为他们设置 `[` 和 `]` 快捷键。

这在 Windows 下是默认的，不过 Mac系统并不是默认的。 